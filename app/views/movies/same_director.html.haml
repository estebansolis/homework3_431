%h2 Movies with same director as (#{@movie.director}) as #{@movie.title}

%table#movies
  %thead
    %tr
      %th Movie Title
      %th Director
      %th Release Dat
      %th Rating
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.director
        %td= movie.release_date
        %td= movie.rating
        %td= link_to "More about #{movie.title}", same_director_path(movie)

= link_to 'Back to movie list', movies_path